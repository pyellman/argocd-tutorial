apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: nginx-rollouts
spec:
  replicas: 5
  strategy:
    canary:
      steps:
      - setWeight: 20
      - pause: {}
      - setWeight: 40
      - pause: {duration: 10s}
      - setWeight: 60
      - pause: {duration: 20s}
      - setWeight: 80
      - pause: {duration: 1m}
  selector:
    matchLabels:
      app: nginx-rollouts
  template:
    metadata:
      labels:
        app: nginx-rollouts
    spec:
      containers:
      - name: nginx-rollouts
        # image: 192.168.1.2:8085/nginx:130a5b21-3166-4ab7-a27c-51f64a198f3
        # my local docker kind-registry at kind-registry:5000 is aliased to localhost:5001 in the kind cluster containerd config
        # image: localhost:5001/nginx:e4237bdf-44e7-4468-84cf-0147c97b2d59
        image: localhost:5001/hello-app:1.0
        ports:
        # - containerPort: 80
        # hello app is on 8080
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  ports:
  - port: 8080
    # targetPort: 80
    targetPort: 8080
  selector:
    app: nginx-rollouts
